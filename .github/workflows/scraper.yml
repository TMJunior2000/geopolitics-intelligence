name: Geopolitics Scraper

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:
    inputs:
      mode:
        default: "BACKFILL"

jobs:
  run-worker:
    # Usiamo Ubuntu 22.04 per massima compatibilità con WARP
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          # yt-dlp: Il re dei downloader
          # pysocks: Fondamentale per il proxy
          pip install yt-dlp requests google-api-python-client google-genai supabase pandas pysocks

      # 🛡️ INSTALLAZIONE MANUALE WARP (Proxy Mode)
      - name: Install & Configure WARP
        run: |
          # 1. Repo Cloudflare
          curl -fsSL https://pkg.cloudflareclient.com/pubkey.gpg | sudo gpg --yes --dearmor --output /usr/share/keyrings/cloudflare-warp-archive-keyring.gpg
          echo "deb [arch=amd64 signed-by=/usr/share/keyrings/cloudflare-warp-archive-keyring.gpg] https://pkg.cloudflareclient.com/ $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/cloudflare-client.list
          
          # 2. Installazione
          sudo apt-get update && sudo apt-get install -y cloudflare-warp

          # 3. Configurazione Proxy SOCKS5 sulla porta 40000
          echo "yes" | sudo warp-cli --accept-tos registration new
          sudo warp-cli --accept-tos mode proxy
          sudo warp-cli --accept-tos proxy port 40000
          
          # 4. Connessione
          sudo warp-cli --accept-tos connect
          
          # Attesa e check veloce
          sleep 5
          curl -x socks5h://127.0.0.1:40000 -I https://www.google.com || echo "⚠️ Proxy check warning (ma yt-dlp riproverà)"

      - name: Run worker
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
        run: python worker.py